machine:
  node:
    version: 4
  services:
    - mongodb

dependencies:
  override:
    - sudo apt-get update; sudo apt-get install g++-4.9 xvfb libgles2-mesa-dev
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - xvfb-run -a -s '-screen 0 800x600x24' npm run test

